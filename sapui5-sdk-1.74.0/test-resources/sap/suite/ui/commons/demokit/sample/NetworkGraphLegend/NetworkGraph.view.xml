<mvc:View
		controllerName="sap.suite.ui.commons.sample.NetworkGraphLegend.NetworkGraph"
		xmlns="sap.suite.ui.commons.networkgraph"
		xmlns:mvc="sap.ui.core.mvc"
		xmlns:l="sap.ui.layout"
		xmlns:core="sap.ui.core"
		xmlns:m="sap.m"
		height="100%">
	<l:FixFlex vertical="true">
		<l:fixContent>
			<m:OverflowToolbar>
				<m:content>
					<m:Select id="orientation" tooltip="Orientation" change="legendChange">
						<core:Item key="Default" text="Default"/>
						<core:Item key="Custom" text="Custom"/>
					</m:Select>
				</m:content>
			</m:OverflowToolbar>
		</l:fixContent>
		<l:flexContent>
			<Graph
					enableWheelZoom="false"
					orientation="{settings>/orientation}"
					nodes="{/nodes}"
					lines="{/lines}"
					groups="{/groups}"
					id="graph">
				<layoutAlgorithm>
				</layoutAlgorithm>
				<nodes>
					<Node
							key="{key}"
							title="{title}"
							icon="{icon}"
							group="{group}"
							attributes="{path:'attributes', templateShareable:true}"
							shape="{shape}"
							status="{status}">
						<attributes>
							<ElementAttribute
									label="{label}"
									value="{value}"/>
						</attributes>
					</Node>
				</nodes>
				<lines>
					<Line
							arrowPosition="{settings>/arrowPosition}"
							arrowOrientation="{settings>/arrowOrientation}"
							from="{from}"
							to="{to}"
							status="{status}">
					</Line>
				</lines>
				<groups>
					<Group
							key="{key}"
							title="{title}"
							icon="sap-icon://action-settings">
					</Group>
				</groups>
			</Graph>
		</l:flexContent>
	</l:FixFlex>
</mvc:View>
